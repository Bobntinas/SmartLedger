# Normalize all text files to LF in the repository
* text=auto eol=lf

# Windows-specific files that must use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary files — no line ending conversion
*.png binary
*.jpg binary
*.ico binary
*.zip binary
*.dll binary